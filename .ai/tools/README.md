# Tools Directory

This directory defines external tools and scripts that the agents can invoke to perform actions.

Since this architecture assumes integration with Claude Code, these might be wrapper scripts or OpenAPI definitions for:
- `jira_client`
- `confluence_client`
- `git_wrapper`
- `github_cli`
